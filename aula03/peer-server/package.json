{
  "name": "peer-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "PORT=9000 npm start",
    "devrun": "npx peerjs --port 9000 --key peerjs --path /",
    "start": "npx peerjs --port $PORT --key peerjs --path /",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "peer": "^0.6.1"
  }
}
